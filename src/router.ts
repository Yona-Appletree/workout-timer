import { createBrowserRouter } from 'react-router-dom';
import { routes } from 'virtual:routes';

// from https://vite-remix-router.vercel.app/guides/getting-started.html
export const router = createBrowserRouter(routes);
